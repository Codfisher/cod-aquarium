---
description: 從 Attribute 到 Varying，搞懂 Vertex Shader 怎麼控制每個頂點的位置、大小與顏色！(｢･ω･)｢✧
tags: ['Shader', 'WebGL', 'GLSL', 'JavaScript']
image: https://codlin.me/shader-notes.webp
pubDate: 20260407
draft: true
---

<script setup>
import { defineAsyncComponent } from 'vue'
import {
  PRESET_ATTRIBUTE,
  PRESET_POINT_SIZE,
  PRESET_VARYING,
  PRESET_COMBINED,
  drawModePresetList,
} from './vertex-shader/shader-preset'

const ShaderPlayground = defineAsyncComponent(() => import('./shader-playground.vue'))
const DiagramAttribute = defineAsyncComponent(() => import('./vertex-shader/diagram-attribute.vue'))
const DiagramVarying = defineAsyncComponent(() => import('./vertex-shader/diagram-varying.vue'))
</script>

![shader-notes](/shader-notes.webp){.cover}

# 鱈魚的 Shader 筆記 - EP02：認識 Vertex Shader

大家好，我是鱈魚。%(｡･∀･)ﾉﾞ%

[上一篇](./260402.01-shader-intro) 我們認識了 Fragment Shader，學會讓每個像素自己決定顏色。

不過當時的頂點都是固定的全螢幕四邊形，Vertex Shader 只負責把座標原封不動丟出去，毫無存在感。%(´ー`)%

現在讓我們來認識 Vertex Shader，學會怎麼控制每個頂點的位置、大小和顏色。

先來看看最終成品：

<ShaderPlayground
  :initial-code="PRESET_COMBINED.code"
  :vertex-code="PRESET_COMBINED.vertexCode"
  :geometry="PRESET_COMBINED.geometry"
  show-vertex-editor
  height="25vh"
/>

畫面上每個點的位置、大小、顏色全都不一樣，這些全靠 Vertex Shader 搞定。

要做到這個效果，我們需要搞懂四個核心概念：

1. **Attribute**：讓每個頂點擁有專屬資料
2. **繪圖模式**：決定頂點要連成什麼形狀
3. **gl_PointSize**：控制點的大小
4. **Varying**：讓資料從 Vertex Shader 傳到 Fragment Shader

讓我們一關一關來。%(•̀ω•́)✧%

## 第一關：Attribute

### 為什麼需要 Attribute？

EP01 介紹過 Uniform，它從 JS 傳資料到 Shader，特色是**每個頂點拿到的值都一樣**。

但如果要讓每個頂點出現在不同位置呢？用 Uniform 做不到，因為所有頂點都會拿到同一組座標，全部擠在一起。

這時候就需要 **Attribute**。

<DiagramAttribute />

| 特性 | Uniform | Attribute |
|------|---------|-----------|
| 資料來源 | JS 傳入 | JS 傳入 |
| 每個頂點的值 | 全部一樣 | 各自不同 |
| 宣告方式 | `uniform vec2 u_xxx` | `attribute vec2 a_xxx` |
| 常見用途 | 時間、滑鼠位置、畫布尺寸 | 頂點座標、顏色、大小 |

簡單說，Uniform 是廣播，Attribute 是點名發資料。%(・∀・)９%

### JS 端怎麼傳 Attribute？

EP01 其實已經用過 Attribute 了，只是沒特別介紹。回顧一下當時的 JS 程式碼：

```js
// 1. 建立 Buffer（GPU 的記憶體空間）
const buffer = gl.createBuffer()

// 2. 綁定到 ARRAY_BUFFER
gl.bindBuffer(gl.ARRAY_BUFFER, buffer)

// 3. 把資料塞進去
const positionList = new Float32Array([
  -1, -1,   // 頂點 1：左下
   1, -1,   // 頂點 2：右下
  -1,  1,   // 頂點 3：左上
])
gl.bufferData(gl.ARRAY_BUFFER, positionList, gl.STATIC_DRAW)

// 4. 告訴 GPU「a_position 在哪、怎麼讀」
const location = gl.getAttribLocation(program, 'a_position')
gl.enableVertexAttribArray(location)
gl.vertexAttribPointer(
  location,
  2,         // 每個頂點讀 2 個 float（x, y）
  gl.FLOAT,
  false,
  0,
  0
)
```

流程就是：建立 Buffer → 綁定 → 塞資料 → 告訴 GPU 怎麼讀。

`vertexAttribPointer` 的第二個參數 `2` 代表「每個頂點讀 2 個 float」，對應 Shader 裡的 `vec2`。如果是 `vec3` 就填 `3`，`float` 就填 `1`。

<br>

路人：「等一下，`gl.STATIC_DRAW` 是什麼意思？」

鱈魚：「就是跟 GPU 說這筆資料不會常常更新，讓它放到快取最佳化。如果會頻繁更新，就用 `gl.DYNAMIC_DRAW`。不過用錯也不會壞，只是效能會差一點 %(´・ω・`)%」

<br>

### 動手試試

下方的 Vertex Shader 有 5 個頂點，用 `gl.POINTS` 畫出來。

試著切到 Vertex Shader 分頁，把 `gl_PointSize` 改大一點看看。%(*´∀`)~♥%

<ShaderPlayground
  :initial-code="PRESET_ATTRIBUTE.code"
  :vertex-code="PRESET_ATTRIBUTE.vertexCode"
  :geometry="PRESET_ATTRIBUTE.geometry"
  show-vertex-editor
  height="25vh"
/>

## 第二關：繪圖模式

### 頂點怎麼連成形狀？

有了頂點座標，接下來 GPU 要決定「這些點怎麼連」。

這由 `gl.drawArrays(mode, first, count)` 的第一個參數 `mode` 決定。EP01 提過 `POINTS`、`LINES`、`TRIANGLES`，這裡完整介紹所有模式。

**點**

`gl.POINTS`：每個頂點畫一個點。

**線**

| 模式 | 規則 | 說明 |
|------|------|------|
| `LINES` | 每 2 個一組 | 1-2 一條、3-4 一條，奇數個會忽略最後一個 |
| `LINE_STRIP` | 前後相連 | 1-2、2-3、3-4... 像折線圖 |
| `LINE_LOOP` | 前後相連 + 首尾相接 | 和 LINE_STRIP 一樣，最後再從尾連回頭 |

**三角形**

| 模式 | 規則 | 說明 |
|------|------|------|
| `TRIANGLES` | 每 3 個一組 | 1-2-3 一個、4-5-6 一個，不共用頂點 |
| `TRIANGLE_STRIP` | 前後共用 | 1-2-3、2-3-4、3-4-5... 每個新頂點和前兩個組成三角形 |
| `TRIANGLE_FAN` | 扇形展開 | 1-2-3、1-3-4、1-4-5... 第一個頂點是共用的中心 |

<br>

路人：「好多模式，要全部背起來嗎？%(ﾟ∀ﾟ)%」

鱈魚：「不用，90% 的時候用 `TRIANGLES` 就對了。其他模式了解就好，面試也不會考 %(́⊙◞౪◟⊙‵)%」

<br>

### 動手試試

下方用同一組五邊形頂點，切換不同繪圖模式看看效果差異。

注意 `LINES` 模式只有 4 個頂點能配對（5 不能整除 2），所以最後一個頂點會被忽略。

<ShaderPlayground
  :initial-code="drawModePresetList[0].code"
  :vertex-code="drawModePresetList[0].vertexCode"
  :geometry="drawModePresetList[0].geometry"
  :preset-list="drawModePresetList"
  show-vertex-editor
  height="25vh"
/>

## 第三關：gl_PointSize

### 控制點的大小

`gl.POINTS` 模式下，每個點預設只有 1 像素，幾乎看不到。

在 Vertex Shader 裡可以透過內建變數 `gl_PointSize` 設定點的大小（單位是像素）：

```glsl
void main() {
  gl_Position = vec4(a_position, 0.0, 1.0);
  gl_PointSize = 20.0;  // 每個點 20px
}
```

不過這樣每個點都一樣大，有點無聊。

如果想讓每個點大小不同，只要再加一個 Attribute 就好：

```glsl
attribute vec2 a_position;
attribute float a_size;    // 每個頂點專屬的大小

void main() {
  gl_Position = vec4(a_position, 0.0, 1.0);
  gl_PointSize = a_size;   // 用 Attribute 的值
}
```

JS 端再多開一個 Buffer 塞大小資料：

```js
const sizeList = new Float32Array([20.0, 35.0, 15.0, 45.0, 28.0])
// 建立 Buffer、綁定、塞資料、設定 vertexAttribPointer...
// 流程和 a_position 一樣，只是 size 參數改成 1（每個頂點讀 1 個 float）
```

<br>

路人：「所以 Attribute 可以有很多個？」

鱈魚：「沒錯，位置、大小、顏色、透明度，想傳幾組就開幾組 Buffer。不過 GPU 有上限，通常至少 16 個，夠用了 %(ゝ∀・)b%」

<br>

### gl_PointCoord：點內部的座標

`gl.POINTS` 模式有個限定的 Fragment Shader 內建變數：`gl_PointCoord`。

它代表目前像素在該點正方形區域內的相對座標，範圍 `0.0 ~ 1.0`。

利用 `gl_PointCoord` 可以在方形的點裡畫出圓形：

```glsl
void main() {
  vec2 center = gl_PointCoord - 0.5;
  float dist = length(center);

  // 丟棄圓外的像素
  if (dist > 0.5) discard;

  gl_FragColor = vec4(0.2, 0.8, 0.6, 1.0);
}
```

`discard` 是 GLSL 的關鍵字，作用是「這個像素不畫了，直接跳過」。

<br>

路人：「等等，EP01 不是說要避免 `if` 嗎？%(◜௰◝)%」

鱈魚：「好問題！`discard` 比較特殊，它本身就會造成 branch divergence，沒辦法用數學取代。不過 `POINTS` 模式的點通常很小，影響範圍有限，所以實務上可以接受 %(・∀・)%」

<br>

### 動手試試

下方範例用了 `a_position` 和 `a_size` 兩個 Attribute，搭配 `gl_PointCoord` 畫出柔和邊緣的圓點。

試著修改 Fragment Shader 裡的 `smoothstep` 參數，看看邊緣柔化程度的變化。%(*´ω`)人(´ω`*)%

<ShaderPlayground
  :initial-code="PRESET_POINT_SIZE.code"
  :vertex-code="PRESET_POINT_SIZE.vertexCode"
  :geometry="PRESET_POINT_SIZE.geometry"
  show-vertex-editor
  height="25vh"
/>

## 第四關：Varying

### 從 Vertex 到 Fragment 的橋樑

到目前為止，Fragment Shader 裡的顏色都是寫死的。如果想讓每個頂點有不同顏色呢？

顏色是 Fragment Shader 的工作，但「每個頂點不同」的資料只有 Attribute 能做到，而 Attribute 只能在 Vertex Shader 裡使用。

怎麼辦？用 **Varying** 當作中間人。%(•̀ω•́)✧%

```glsl
// ── Vertex Shader ──
attribute vec2 a_position;
attribute vec3 a_color;
varying vec3 v_color;      // 宣告 varying

void main() {
  gl_Position = vec4(a_position, 0.0, 1.0);
  v_color = a_color;        // 把顏色交給 varying
}
```

```glsl
// ── Fragment Shader ──
precision mediump float;
varying vec3 v_color;       // 同名的 varying

void main() {
  gl_FragColor = vec4(v_color, 1.0);
}
```

兩邊都宣告同名同型別的 `varying`，Vertex Shader 寫入，Fragment Shader 讀取。

### GPU 自動插值

Varying 最神奇的地方在於 GPU 會自動做**線性插值**。

假設三角形的三個頂點分別設定紅、綠、藍三種顏色，三角形內部的每個像素會根據自己和三個頂點的距離，自動混合出過渡色。

<DiagramVarying />

不需要寫任何混色的程式碼，GPU 會自動根據像素位置計算出對應比例。這就是為什麼 OpenGL 入門教學常常以「彩色三角形」當作 Hello World。%(ゝ∀・)b%

<br>

路人：「所以 Varying 不只能傳顏色？」

鱈魚：「任何資料都可以，UV 座標、法線方向、光照強度，只要是 `float`、`vec2`、`vec3`、`vec4` 都行。傳什麼都會被自動插值 %(・∀・)９%」

<br>

### 動手試試

經典的彩色三角形，三個頂點分別是紅、綠、藍。

切到 Vertex Shader 分頁，試著把 `v_color = a_color` 改成 `v_color = vec3(1.0, 1.0, 0.0)` 看看，所有像素都會變成同一色（因為不再使用 Attribute 的值）。

<ShaderPlayground
  :initial-code="PRESET_VARYING.code"
  :vertex-code="PRESET_VARYING.vertexCode"
  :geometry="PRESET_VARYING.geometry"
  show-vertex-editor
  height="25vh"
/>

## 組合技：回到成品

學完四關，再回頭看看開頭的成品。切到 Vertex Shader 分頁，逐行解讀：

```glsl
attribute vec2 a_position;   // Attribute：每個頂點的座標
attribute float a_size;      // Attribute：每個頂點的大小
attribute vec3 a_color;      // Attribute：每個頂點的顏色

varying vec3 v_color;        // Varying：傳給 Fragment Shader

void main() {
  gl_Position = vec4(a_position, 0.0, 1.0);
  gl_PointSize = a_size;     // 控制點的大小
  v_color = a_color;         // 把顏色交出去
}
```

Fragment Shader 負責用 `gl_PointCoord` 把方形的點畫成圓形，再從 `v_color` 取得顏色：

```glsl
precision mediump float;

varying vec3 v_color;

void main() {
  vec2 center = gl_PointCoord - 0.5;
  float dist = length(center);

  if (dist > 0.5) discard;

  float alpha = 1.0 - smoothstep(0.3, 0.5, dist);
  gl_FragColor = vec4(v_color, alpha);
}
```

<ShaderPlayground
  :initial-code="PRESET_COMBINED.code"
  :vertex-code="PRESET_COMBINED.vertexCode"
  :geometry="PRESET_COMBINED.geometry"
  show-vertex-editor
  height="25vh"
/>

### 挑戰時間

試試看能不能改出這些效果：

1. 讓點的位置隨 `u_time` 移動（提示：`gl_Position.x += sin(u_time)` 之類的）
2. 讓點的大小隨時間脈動（提示：`gl_PointSize = a_size + sin(u_time) * 10.0`）
3. 讓顏色隨時間變化（提示：在 Fragment Shader 裡對 `v_color` 加上 `sin(u_time)` 的偏移）

## 總結 🐟

- **Attribute** 讓每個頂點擁有專屬資料（位置、大小、顏色等），和 Uniform 的「全部一樣」互補
- **繪圖模式** 決定 GPU 怎麼連接頂點：POINTS、LINES、TRIANGLES 等，90% 的時候用 TRIANGLES 就好
- **gl_PointSize** 在 POINTS 模式下控制點的大小，搭配 `gl_PointCoord` 可以在點內部畫圓
- **Varying** 是 Vertex Shader 傳資料給 Fragment Shader 的橋樑，GPU 會自動做線性插值

下一篇我們會用這些概念組合出真正的粒子系統，讓大量的點動起來，產生煙霧飄散的效果。敬請期待！%(ノ>ω<)ノ%

以上就是 Vertex Shader 核心概念的介紹，有錯誤還請多多指教。

感謝您讀到這裡，如果您覺得有收穫，歡迎分享出去。%(*´∀`)~♥%
