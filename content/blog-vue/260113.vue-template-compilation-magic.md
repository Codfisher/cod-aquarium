---
description: 常聽別人說 Vue template 有編譯魔法，來仔細看看到底差在哪吧。
tags: ["Vue"]
image: https://codlin.me/vue-template-compilation-magic.webp
pubDate: 20260113
---

<script setup>
  import PerfComparison from './vue-template-compilation-magic/perf-comparison.vue'
</script>

![vue-template-compilation-magic](/vue-template-compilation-magic.webp){.cover}

# Vue template 編譯魔法

和同事討論到 Vue template、`h()` 的使用情境剛好最近使用 Nuxt UI 也常使用 `h()` 來渲染內容。

在高度動態的場景下，使用 `h()` 會更加方便，先前也有[文章](/blog-vue/vue-h-function-makes-quasar-dialog-easier-to-reuse.html)提到相關應用。

::: tip 甚麼是 `h()`
`h()` 是 Vue 的渲染函式，詳細說明可以[看看官方文件](https://vuejs.org/guide/extras/render-function.html)
:::

但是 `h()` 不會有 template 的編譯最佳化，所以沒有特別需求的情境下，還是建議使用 template。

[官方文件](https://vuejs.org/guide/essentials/template-syntax.html)提到：

> If you are familiar with Virtual DOM concepts and prefer the raw power of JavaScript, you can also directly write render functions instead of templates, with optional JSX support. However, do note that they do not enjoy the same level of compile-time optimizations as templates.

聽說歸聽說，~~忍者龜忍者~~，從來沒有仔細研究過到底差在哪，趁這次機會來看看吧。%( ´ ▽ ` )ﾉ%

## 速度真的有差？

<perf-comparison />

## 編譯結果

要怎麼比較差異呢？實際上不管是 template 還是 `h()`，最終 Vue 都會編譯成 JS 程式碼，用以產生 Virtual DOM (VNode)

Virtual DOM 部分不是本文的重點，未來有興趣時再來研究看看，所以這次來比對編譯後的產物，看看有甚麼差別吧。

這裡使用 [Vue SFC Playground](https://play.vuejs.org/) 的 PROD 模式，來比較 template、`h()` 與 JSX 的編譯內容。

## 總結 🐟
